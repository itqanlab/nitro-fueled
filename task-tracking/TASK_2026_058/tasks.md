# Development Tasks - TASK_2026_058

**Total Tasks**: 17 | **Batches**: 5 | **Status**: 0/5 complete

---

## Plan Validation Summary

**Validation Status**: PASSED WITH RISKS

### Assumptions Verified

- 60 task folders exist in `task-tracking/` — Verified (ls count = 60)
- `registry.md` has accurate status values for all 60 tasks — Verified (read registry.md, all 60 rows present)
- `parseRegistry()` regex requires `| TASK_...` prefix — assumption is that comment line is skipped naturally; no code change needed
- `packages/cli/scaffold/` contains full copies of SKILL.md files — Unverified; developer must confirm before Phase 8

### Risks Identified

| Risk | Severity | Mitigation |
|------|----------|------------|
| Scaffold SKILL.md files may be trimmed copies, not full copies | MED | Task 5.1 requires developer to check line count before editing |
| `FIXING` state missing from VALID_STATUSES in registry.ts | MED | Task 2.1 explicitly requires adding FIXING to valid values list |
| Backfill misses a task folder (count mismatch) | LOW | Task 1.2 includes explicit count verification step |

---

## Batch 1: Backfill Existing Task Folders - COMPLETE

**Developer**: systems-developer
**Tasks**: 3 | **Dependencies**: None — must be committed before any other batch

### Task 1.1: Write status files for all 63 existing task folders COMPLETE

**File**: `task-tracking/TASK_*/status` (63 files)
**Status**: COMPLETE

**What to do**:
Read `task-tracking/registry.md` (already read — all 60 rows with statuses are known). For each `TASK_YYYY_NNN` folder, write a `status` file containing the exact status word from registry column 2.

Status values to write (from registry.md):
- TASK_2026_001 through TASK_2026_005: `COMPLETE`
- TASK_2026_006: `CANCELLED`
- TASK_2026_007 through TASK_2026_017: `COMPLETE`
- TASK_2026_018 through TASK_2026_019: `COMPLETE`
- TASK_2026_020 through TASK_2026_024: `COMPLETE`
- TASK_2026_025 through TASK_2026_032: `COMPLETE`
- TASK_2026_033: `CANCELLED`
- TASK_2026_034 through TASK_2026_037: `COMPLETE`
- TASK_2026_038 through TASK_2026_041: `CREATED`
- TASK_2026_042 through TASK_2026_048: `COMPLETE`
- TASK_2026_049 through TASK_2026_051: `CREATED`
- TASK_2026_052: `COMPLETE`
- TASK_2026_053 through TASK_2026_057: `CREATED`
- TASK_2026_058: `IN_PROGRESS`
- TASK_2026_059 through TASK_2026_060: `CREATED`

**Quality Requirements**:
- Plain text, one word, NO trailing newline
- Use Write tool with exact content string (no `\n` at end)
- Valid values: `CREATED | IN_PROGRESS | IMPLEMENTED | IN_REVIEW | FIXING | COMPLETE | FAILED | BLOCKED | CANCELLED`

---

### Task 1.2: Verify backfill completeness COMPLETE

**File**: N/A (verification step)
**Status**: COMPLETE

**What to do**:
Run both count commands and confirm they match:
```bash
ls -d /Volumes/SanDiskSSD/mine/nitro-fueled/task-tracking/TASK_*/ | wc -l
ls /Volumes/SanDiskSSD/mine/nitro-fueled/task-tracking/TASK_*/status | wc -l
```
Both must return `60`. If they differ, identify the missing folder and write the missing status file.

---

### Task 1.3: Add generated header to registry.md COMPLETE

**File**: `/Volumes/SanDiskSSD/mine/nitro-fueled/task-tracking/registry.md`
**Status**: COMPLETE

**What to do**:
Prepend the following line as the very first line of `registry.md` (before `# Task Registry`):
```
<!-- DO NOT EDIT — generated by nitro-fueled status -->
```

This signals the file is now a derived artifact. Content below the header is unchanged.

---

**Batch 1 Verification**:
- All 60 `status` files exist (count check)
- `registry.md` has the generated header as line 1
- Build check: N/A (no TypeScript changes)
- code-logic-reviewer approved

---

## Batch 2: CLI Registry Regeneration - PENDING

**Developer**: backend-developer
**Tasks**: 2 | **Dependencies**: Batch 1 must be COMPLETE

### Task 2.1: Add generateRegistry() to packages/cli/src/utils/registry.ts PENDING

**File**: `/Volumes/SanDiskSSD/mine/nitro-fueled/packages/cli/src/utils/registry.ts`
**Spec Reference**: implementation-plan.md Phase 2, Step 2.1
**Status**: PENDING

**What to do**:
Add a new exported function `generateRegistry(cwd: string): void` that:
1. Globs `task-tracking/TASK_*/` directories (sorted ascending by Task ID)
2. For each folder, reads `status` file for current state; if missing, falls back to reading the matching row from `registry.md` column 2 and logs a warning to stderr
3. Reads `task.md` for metadata: Type, Description, Created date, Model
4. Assembles a registry table row in the same format as the existing table
5. Writes `task-tracking/registry.md` with the `<!-- DO NOT EDIT — generated by nitro-fueled status -->` header followed by the full table

**Quality Requirements**:
- Follow the existing utility style in `registry.ts` (read the file first to match patterns)
- `FIXING` state MUST be in the `VALID_STATUSES` constant (add it if missing)
- Trim whitespace from `status` file content before using value
- Graceful fallback: if `status` file is missing, read from registry column 2, log warning
- Function must be exported

---

### Task 2.2: Modify registerStatusCommand to call generateRegistry first PENDING

**File**: `/Volumes/SanDiskSSD/mine/nitro-fueled/packages/cli/src/commands/status.ts`
**Spec Reference**: implementation-plan.md Phase 2, Step 2.2
**Status**: PENDING

**What to do**:
In the `registerStatusCommand` function, add a `generateRegistry(cwd)` call immediately before the existing `parseRegistry(cwd)` call. The chain becomes:
1. `generateRegistry(cwd)` — scan status files, rewrite registry.md
2. `parseRegistry(cwd)` — read the freshly generated registry.md
3. Render output (unchanged)

Import `generateRegistry` from `../utils/registry` (same import path as `parseRegistry`).

**Quality Requirements**:
- No other logic changes — only insert the one call before `parseRegistry`
- Read the file first to find the exact location

---

**Batch 2 Verification**:
- `generateRegistry` function exported from `registry.ts`
- `FIXING` state present in valid values
- `status.ts` calls `generateRegistry` before `parseRegistry`
- Build passes: `npx nx build cli` from `/Volumes/SanDiskSSD/mine/nitro-fueled`
- code-logic-reviewer approved

---

## Batch 3: Commands — project-status and create-task - PENDING

**Developer**: systems-developer
**Tasks**: 2 | **Dependencies**: Batch 1 must be COMPLETE

### Task 3.1: Add registry regeneration step to /project-status command PENDING

**File**: `/Volumes/SanDiskSSD/mine/nitro-fueled/.claude/commands/project-status.md`
**Spec Reference**: implementation-plan.md Phase 3, Step 3.1
**Status**: PENDING

**What to do**:
In the Phase 1 data gathering section of the command, add a new Step 0 (before reading registry.md) that instructs the agent to regenerate `registry.md`:

> **Step 0: Regenerate registry.md**
> Before reading registry.md, regenerate it by scanning all `task-tracking/TASK_*/status` and `task-tracking/TASK_*/task.md` files. For each task folder (sorted by Task ID), read the `status` file for current state (fall back to registry column 2 if missing), read `task.md` for metadata. Write `task-tracking/registry.md` with the `<!-- DO NOT EDIT — generated by nitro-fueled status -->` header and the full table. Then proceed with reading the regenerated registry.md for task data.

Read the file first to understand the exact structure and insert the step in the right place.

---

### Task 3.2: Replace Step 5 in /create-task command with status file write PENDING

**File**: `/Volumes/SanDiskSSD/mine/nitro-fueled/.claude/commands/create-task.md`
**Spec Reference**: implementation-plan.md Phase 4, Steps 4.1 and 4.2
**Status**: PENDING

**What to do**:

**Step 4.1**: Replace the current Step 5 (which appends a row to `registry.md`) entirely with:

> **Step 5: Write status file**
> Write `task-tracking/TASK_YYYY_NNN/status` with the single word `CREATED` (no trailing newline).
>
> The registry is no longer appended to during task creation. It is regenerated on demand by `nitro-fueled status` and `/project-status`. The Task ID for the new task is still determined by scanning `registry.md` for the highest existing NNN — this read is still valid.

**Step 4.2**: In the Important Rules section, remove or replace the rule about the registry row format with: `"Write status file immediately after creating the task folder: task-tracking/TASK_YYYY_NNN/status containing CREATED"`.

Read the file first to locate the exact text.

---

**Batch 3 Verification**:
- `project-status.md` has Step 0 regeneration instruction before registry read
- `create-task.md` Step 5 writes status file, does NOT append to registry
- `create-task.md` Important Rules section updated
- code-logic-reviewer approved

---

## Batch 4: Auto-Pilot and Orchestration Skills - PENDING

**Developer**: systems-developer
**Tasks**: 5 | **Dependencies**: Batch 1 must be COMPLETE

### Task 4.1: Update orchestration SKILL.md — Completion Phase and Exit Gates PENDING

**File**: `/Volumes/SanDiskSSD/mine/nitro-fueled/.claude/skills/orchestration/SKILL.md`
**Spec Reference**: implementation-plan.md Phase 5, Steps 5.1, 5.2, 5.3
**Status**: PENDING

**What to do**:

**Step 5.1** — Replace the Completion Phase Section 2 (around line 374):
- Find: `### 2. Update Registry` / `Update task-tracking/registry.md — set status to COMPLETE`
- Replace with: `### 2. Write Status File` / `Write task-tracking/TASK_[ID]/status with the single word COMPLETE (no trailing newline).` / `Do NOT write to registry.md — the registry is a generated artifact regenerated by nitro-fueled status.`

**Step 5.2** — Replace Exit Gate registry checks:
- Build Worker Exit Gate: replace both registry rows with `status` file checks (read `task-tracking/TASK_[ID]/status`, confirm contains IMPLEMENTED; check git status shows status file committed)
- Review Lead Exit Gate: replace registry row with `status` file check

**Step 5.3** — NEW_TASK initialization: after the step that creates the task folder, add: `Write task-tracking/TASK_[ID]/status with CREATED.`

Read the full file first to locate exact line numbers and text.

---

### Task 4.2: Update auto-pilot SKILL.md — Step 2 state reads PENDING

**File**: `/Volumes/SanDiskSSD/mine/nitro-fueled/.claude/skills/auto-pilot/SKILL.md`
**Spec Reference**: implementation-plan.md Phase 6, Steps 6.1, 6.3, 6.7
**Status**: PENDING

**What to do**:

**Step 6.1** — Update Step 2 (Read Registry and Task Folders):
New behavior: (1) Read `registry.md` for task list (Task ID, Type, Description — metadata only). (2) For each task ID, read `task-tracking/TASK_YYYY_NNN/status` for the current state. (3) If `status` file is missing, fall back to registry column 2 and log warning: `"TASK_X: status file missing, reading state from registry.md"`.

**Step 6.3** — Update Registry Write Safety section (lines 80-88):
Replace the race condition description with: Workers write their state update to `task-tracking/TASK_YYYY_NNN/status` as their LAST action before exiting. Each worker writes only to its own task folder — no cross-task conflicts, no re-read-before-write needed. The race condition described here no longer applies.

**Step 6.7** — Update Step 7 (Handle Completions) state detection:
Replace reads of `registry.md` for task state with reads of `task-tracking/TASK_YYYY_NNN/status`.

Read the full file first to locate exact sections.

---

### Task 4.3: Update auto-pilot SKILL.md — BLOCKED writes PENDING

**File**: `/Volumes/SanDiskSSD/mine/nitro-fueled/.claude/skills/auto-pilot/SKILL.md`
**Spec Reference**: implementation-plan.md Phase 6, Step 6.2
**Status**: PENDING

**What to do**:

**Step 6.2** — Update Step 3 (dependency graph) BLOCKED writes:
Find all places where the Supervisor itself writes `BLOCKED` to `registry.md` (around lines 387, 391, 395). Replace each with: Write `task-tracking/TASK_YYYY_NNN/status` containing `BLOCKED`.

Read the file first to identify all occurrences.

---

### Task 4.4: Update auto-pilot SKILL.md — Build Worker prompt templates PENDING

**File**: `/Volumes/SanDiskSSD/mine/nitro-fueled/.claude/skills/auto-pilot/SKILL.md`
**Spec Reference**: implementation-plan.md Phase 6, Steps 6.4, 6.5, 6.6
**Status**: PENDING

**What to do**:

**Step 6.4** — First-Run Build Worker Prompt (around lines 967-984):
- Replace registry IN_PROGRESS write instruction with: `Write task-tracking/TASK_YYYY_NNN/status with the single word IN_PROGRESS (no trailing newline). This signals the Supervisor that work has begun.`
- Replace registry IMPLEMENTED write + re-read-before-write instruction with: `c. Write task-tracking/TASK_YYYY_NNN/status with the single word IMPLEMENTED (no trailing newline). This is the FINAL action before exit.` / `d. Commit the status file: docs: mark TASK_YYYY_NNN IMPLEMENTED`
- Update Exit Gate checklist to check `status` file instead of registry

**Step 6.4 (Retry)** — Retry Build Worker Prompt (lines 1021-1047): apply the same replacements as above.

**Step 6.5** — Review Lead Prompt (around line 1077):
Replace `Update task-tracking/registry.md — set status to IN_REVIEW` with: `Write task-tracking/TASK_YYYY_NNN/status with the single word IN_REVIEW (no trailing newline).`

**Step 6.6** — Fix Worker and Completion Worker Prompt Templates (around lines 1259-1362):
Replace all `registry.md to COMPLETE` instructions with: `Write task-tracking/TASK_YYYY_NNN/status with the single word COMPLETE (no trailing newline). This is the FINAL action before exit.`
Remove re-read-before-write instructions from all these prompts.

Read the full file (it is large) before editing to locate exact text.

---

### Task 4.5: Update planner.md — state reads and registry writes PENDING

**File**: `/Volumes/SanDiskSSD/mine/nitro-fueled/.claude/agents/planner.md`
**Spec Reference**: implementation-plan.md Phase 7, Steps 7.1–7.7
**Status**: PENDING

**What to do**:

**Step 7.1** — Section 3a (Product Owner Mode), step that reads registry.md for state:
New: Read `registry.md` for task list (IDs and metadata only), then read each `task-tracking/TASK_YYYY_NNN/status` file for current state.

**Step 7.2** — Section 3b (Status Mode): same change as 7.1.

**Step 7.3** — Section 3e (Backlog Sizing Review):
Replace: `Collect all tasks with status CREATED from registry.md`
With: Read each `task-tracking/TASK_YYYY_NNN/status` file and collect tasks where content is `CREATED`. Use registry only to enumerate task IDs.

**Step 7.4** — Section 4b (Task ID Generation): reading registry for highest NNN remains valid — no change needed.

**Step 7.5** — Section 4e (Registry Update):
Replace the row-append instruction with: Write `task-tracking/TASK_YYYY_NNN/status` with `CREATED`. The registry is no longer appended to by the Planner — it is regenerated by `nitro-fueled status` or `/project-status`.

**Step 7.6** — Section 5d (Staleness Detection):
Replace comparison against `registry.md` statuses with: compare against `TASK_*/status` files. Registry remains readable but treat as informational only.

**Step 7.7** — Section 9 (Orphan Task Detection):
Update description to clarify that status comes from `status` files (not registry column 2); registry is still used for task ID enumeration.

Read the full file first to locate exact section text.

---

**Batch 4 Verification**:
- `orchestration/SKILL.md`: Completion Phase writes `status` file, Exit Gates check `status` file, NEW_TASK creates `status` file
- `auto-pilot/SKILL.md`: Step 2 reads `status` files, BLOCKED writes go to `status` files, Registry Write Safety section updated, all 5 worker prompt templates updated, Step 7a reads `status` files
- `planner.md`: all 7 sections updated (3a, 3b, 3e, 4b unchanged, 4e, 5d, 9)
- code-logic-reviewer approved

---

## Batch 5: Scaffold File Mirrors - PENDING

**Developer**: systems-developer
**Tasks**: 2 | **Dependencies**: Batch 4 must be COMPLETE

### Task 5.1: Update scaffold orchestration SKILL.md PENDING

**File**: `/Volumes/SanDiskSSD/mine/nitro-fueled/packages/cli/scaffold/.claude/skills/orchestration/SKILL.md`
**Spec Reference**: implementation-plan.md Phase 8, Step 8.1
**Status**: PENDING

**Validation Note**: RISK — scaffold file may be a trimmed copy. Read both the source and scaffold files, compare line counts. If the scaffold is trimmed (different structure), flag it and apply equivalent changes to the scaffold structure. Do NOT blindly copy-paste from source if the scaffold has a different format.

**What to do**:
1. Read the scaffold file first and compare its structure to the source at `/Volumes/SanDiskSSD/mine/nitro-fueled/.claude/skills/orchestration/SKILL.md`
2. Apply all Phase 5 changes (Task 4.1) to the scaffold file — same Completion Phase, Exit Gate, and NEW_TASK updates

---

### Task 5.2: Update scaffold auto-pilot SKILL.md PENDING

**File**: `/Volumes/SanDiskSSD/mine/nitro-fueled/packages/cli/scaffold/.claude/skills/auto-pilot/SKILL.md`
**Spec Reference**: implementation-plan.md Phase 8, Step 8.2
**Status**: PENDING

**Validation Note**: Same risk as Task 5.1 — confirm scaffold is a full copy before applying changes.

**What to do**:
1. Read the scaffold file first and compare its structure to the source at `/Volumes/SanDiskSSD/mine/nitro-fueled/.claude/skills/auto-pilot/SKILL.md`
2. Apply all Phase 6 changes (Tasks 4.2–4.4) to the scaffold file — Step 2 state reads, BLOCKED writes, Registry Write Safety section, all 5 worker prompt templates, Step 7a

---

**Batch 5 Verification**:
- Scaffold `orchestration/SKILL.md` mirrors all Phase 5 changes
- Scaffold `auto-pilot/SKILL.md` mirrors all Phase 6 changes
- code-logic-reviewer approved
