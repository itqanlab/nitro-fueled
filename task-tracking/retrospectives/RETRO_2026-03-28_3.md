# Retrospective Report — RETRO_2026-03-28_3

## Scope

3 auto-pilot sessions (last 3 not previously covered):
- **SESSION_2026-03-28_11-13-12** — 7 complete tasks: TASK_2026_082–085, 088, 091, 094
- **SESSION_2026-03-28_16-12-00** — 4 complete tasks: TASK_2026_092, 109, 113, 117
- **SESSION_2026-03-28_16-39-39** — 0 fully COMPLETE tasks (114, 118, 124 still IN_REVIEW at retro time)

Total: **11 completed tasks** analyzed (TASK_2026_091 has N/A review scores — pre-implemented reconcile).

## Quality Trends

| Metric | Value |
|--------|-------|
| Tasks Analyzed | 11 |
| Tasks with Explicit Scores | 10 (091 pre-implemented N/A) |
| Avg Code Style Score | 6.3/10 (082=6, 085=6, 088=7, 092=6, 094=6, 109=7, 113=6; 083/084 FAIL/NEEDS-FIXES, no numeric) |
| Avg Code Logic Score | 6.6/10 (082=8, 085=5, 088=4, 092=7, 094=8, 109=7, 113=7; 083 FAIL/fixed, 084 PASS no numeric) |
| Avg Security Score | 6.9/10 (082=9, 088=2, 092=7, 094=6, 109=~5 CRITICAL+HIGH, 113=9, 117=8.5) |
| Total Blocking Findings (approx) | 22 (083:3, 084:~4, 085:4, 088:2+deferred-auth, 092:7, 094:2, 109:2 deferred, 117:2) |
| Blocking Findings Fixed | ~17 (all except deferred items) |
| Blocking Findings Acknowledged/Deferred | 5 (088 auth-CRITICAL, 088 CORS production, 109 TaskIdParamDto CRITICAL, 109 analytics.dto size, 084 [innerHTML] XSS pattern) |

Notable: TASK_2026_088 security score of 2/10 is the lowest security score seen in this project — driven by missing authentication (deferred as out-of-scope architectural decision), not a fixable bug.

## Recurring Patterns (3+ tasks)

| Pattern | Count | Task IDs | Category |
|---------|-------|----------|----------|
| `as` type assertions in implementation | 4 | 083, 084, 092, 094 | TypeScript conventions |
| File size limit violations (components >150, services >200 lines) | 4 | 084, 085, 092, 109 | Code size |
| Hardcoded hex colors instead of CSS custom properties / design tokens | 3 | 082, 084, 085 | Frontend style |
| Missing route guards (no `canActivate`) — systemic auth gap | 4 | 082, 083, 084, 085 | Security (out-of-scope — no auth system yet) |

## Violated Existing Lessons

| Lesson File | Lesson Summary | Task IDs Violated | Times |
|-------------|---------------|-------------------|-------|
| `review-general.md` | "No `as` type assertions — if the type system fights you, the type is wrong" | 083, 084, 092, 094 | 4 |
| `review-general.md` | "File Size Limits — Components: max 150 lines. Services/repositories: max 200 lines." | 084, 085, 092, 109 | 4 |
| `frontend.md` | "Never hardcoded hex colors — use CSS variables or Tailwind theme tokens" | 082, 084, 085 | 3 |
| `review-general.md` | "Explicit access modifiers on ALL class members — `public`, `private`, `protected`. Never bare." | 092, 094 | 2 (watch — below 3-threshold) |

All four top violations were in lessons that were already written. The rules are known — they are not being pre-checked during implementation.

## Acknowledged-but-Unfixed Findings

| Category | Task IDs | Status |
|----------|----------|--------|
| NestJS WebSocket gateway — no authentication on `handleConnection` | 088, 092 | Out of scope — auth system not yet built; 2 tasks now, accumulating |
| `[innerHTML]` binding for user-supplied / AI content (XSS architectural risk) | 084 | Deferred to API integration phase |
| `TaskIdParamDto` missing `@Matches` / `@IsString` validation + `ValidationPipe` not global | 109 | Deferred to follow-on task |
| `analytics.dto.ts` exceeds 200-line limit (288 lines) | 109 | Deferred to follow-on task |

## Worker Health

| Metric | Value |
|--------|-------|
| Workers Stuck | 3 (TASK_2026_088, TASK_2026_091, TASK_2026_113 — each hit strike 2/2) |
| Workers Killed | 3 |
| Total Retries | 6 (088×1, 091×1, 110×1, 109×1, 113×1, 099×1) |
| GLM → Claude Fallbacks | 2 (091, 113 — glm stuck x2, fallback succeeded with claude/sonnet) |
| Sessions Analyzed | 3 (11-13-12, 16-12-00, 16-39-39) |

All 3 stuck workers were in SESSION_2026-03-28_11-13-12. SESSION_2026-03-28_16-12-00 and 16-39-39 had zero stuck events. The GLM fallback mechanism is working correctly — both GLM-stuck tasks (091, 113) completed successfully after being retried with claude/sonnet.

## Conflicts Detected (PO Decision Required)

None.

## Proposed Lessons (Exceeds Auto-Apply Cap — PO Approval Required)

| Lesson | Source Tasks | Recurrence |
|--------|-------------|------------|
| **NestJS backend.md**: `@WebSocketGateway()` handlers must validate authentication before emitting data. `handleConnection` without an auth token check broadcasts session/cost/task data to any connected client. Gate `handleConnection` with `@UseGuards(AuthGuard)` or an explicit token-validation call before emitting any payload. | 088, 092 | 2 tasks (below 3-threshold; propose for early capture given CRITICAL severity) |
| **backend.md**: `class-validator` and `class-transformer` must be in `dependencies`, not `devDependencies`, when `ValidationPipe` is used. These packages are runtime requirements — placing them in devDeps causes silent breakage in production builds where devDeps are pruned. | 109 | 1 task (propose for early capture) |
| **backend.md**: Register `ValidationPipe` globally in NestJS `main.ts` (`app.useGlobalPipes(new ValidationPipe({ whitelist: true }))`) before any routes use class-validator DTO decorators. Without global registration, `@IsString` / `@Matches` decorators are silently ignored and path params are passed unvalidated. | 109 | 1 task (propose for early capture) |

## Auto-Applied Updates

None — all recurring patterns in this scope are covered by existing lessons. No new entries meet the 3-task threshold for auto-apply.

> **Idempotency tag**: `[RETRO_2026-03-28_3sessions]` — written 2026-03-28.

## Proposed Tasks

| Title | Type | Rationale | Suggested Priority |
|-------|------|-----------|-------------------|
| Fix retrospective commit step ordering — commit fires before auto-apply writes (`review-lessons/`, `anti-patterns.md` not staged in retro commit) | BUGFIX | Identified in TASK_2026_113 logic review as a known non-blocking issue. Every retrospective run leaves lesson/anti-pattern file changes uncommitted. | P2-Medium |
| Add NestJS gateway authentication guard to `dashboard.gateway.ts` | BUGFIX | TASK_2026_088 + TASK_2026_092 both flagged missing `handleConnection` auth as CRITICAL/MEDIUM. Accumulating without resolution. | P2-Medium |
| Resolve deferred TASK_2026_109 findings — `TaskIdParamDto` validation + `analytics.dto.ts` split + `ValidationPipe` global registration | FEATURE | 3 deferred findings from 109 that were marked "follow-on required" at COMPLETE time. | P2-Medium |

## Observations

1. **Rule adherence gap, not knowledge gap** — The three most violated rules (`as` assertions, file size, hex colors) all have mature, well-documented lessons. The violations are not due to missing documentation; they are happening despite it. This suggests the Build Worker prompt needs a stronger pre-implementation checklist step, not more lesson text.

2. **TASK_2026_088 security score (2/10)** is an outlier driven by architectural decisions (no auth, no rate limiting) that are out of scope for the task. The implementation itself was correct; the score reflects the system's current incomplete state. This should not regress further as auth is added.

3. **GLM fallback is proven** — 2/2 GLM-stuck tasks succeeded on claude/sonnet retry. The fallback mechanism (memory: `project_glm_reliability.md`) is working as designed.

4. **Retro commit bug creates incomplete lesson history** — Per TASK_2026_113 logic review: the retro SKILL.md commits before auto-apply writes, so any lessons appended during a retro run are not included in the retro commit. This report's own auto-apply entries (if any) would be affected. Tracked as a proposed task above.
