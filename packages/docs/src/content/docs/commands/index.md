---
title: Commands
description: All CLI commands and slash commands available in Nitro-Fueled.
---

Nitro-Fueled provides both CLI commands (`npx nitro-fueled`) and Claude Code slash commands (`/orchestrate`, `/plan`, `/auto-pilot`).
